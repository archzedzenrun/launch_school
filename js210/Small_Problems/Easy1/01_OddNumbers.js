for (let i = 1; i < 100; i += 2) {
  console.log(i);
}

// Further Exploration:

function oddNumbers(limit) {
  let num = 1;
  while (num <= limit) {
    console.log(num);
    num += 2;
  }
}

oddNumbers(50);