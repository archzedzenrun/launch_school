// 1. Use single quotes instead of double if the quotes doesnt contain single quotes.

// 2. Only use 1 let declaration per variable. Variable names should use camelCase.

// 3. Use strict equality operator.

// 4. Use brackets for multiline if statements.

// 5. Use spaces before and after operators. Only indent 2 spaces. Use explicit string coercion. Avoid ++ and -- operators. Use named function expressions instead of function declarations.