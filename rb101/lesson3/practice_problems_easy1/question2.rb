=begin 
1. != means not equal and is used when comparing the equality of two objects.
2. ! before something like !user_name will check its truthiness and return 
   the opposite.
3. ! after something like words.uniq! is typically a sign that the method is 
   destructive.
4. Syntax error?
5. A ? after something like 'name ?' would be the ternery operator, if its 
   after a method name its just part of the method.
6. !! before something like !!user_name returns an opposite, opposite boolean 
   value.  For example if user_name evaluates to true, !user_name will return 
   false, and !!user_name will return true.
=end
